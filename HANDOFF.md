# 课堂归档 PWA — 交接文档

> 面向接手这个项目的人。写于 2026-09-18。配套阅读：`PLAN.md`（带勾选的活计划，含每个里程碑的验收标准与实施备注）。

## 1. 这是什么

一个给大学生自己用的**课堂照片归档工具**：上课拍 PPT/黑板，回到宿舍在电脑大屏上复习。

解决的具体痛点：一天连上几堂课，拍了一堆照片后**分不清哪张是哪门课的**，课后复习找不到。

- 线上地址：**https://vv82c.github.io/class-list/**
- 仓库：`https://github.com/vv82c/class-list`（公开），源码在 `main` 分支，站点内容在 `gh-pages` 分支
- 形态：本地 Web 工具，**电脑浏览器为主战场**（Chrome/Edge 二选一并固定用一个）；线上另有手机 PWA，已冻结当备胎

真实使用循环：**教室用手机拍（离线也行）→ 回宿舍电脑上看**。这个循环是全部取舍的依据。

> **2026-09-18 定案：PC-first 转向** —— 手机只是"拍照 + 加时间戳"的工具，归档与阅读全部在电脑完成。照片真源 = 电脑浏览器沙箱；手机端 PWA 冻结不再投入（线上留着当备胎）；服务器同步 + 备案路线取消。详见 PLAN.md「阶段3」与「跨设备数据方案」。

## 2. 快速上手

```bash
npm install
npm run dev        # vite --host，手机连同一路由访问 http://<局域网IP>:5173
npm test           # node --import tsx --test，18 个 lib 层单测
npm run typecheck  # tsc --noEmit
npm run build      # tsc + vite build + 生成 SW
# 发布（需 gh 已登录）
npm run build && npx --yes gh-pages -d dist -m "说明"
```

环境：Node 24（v24.19.0）、npm 11。Windows 中文 locale 下无特殊处理需求。

## 3. 技术栈与目录

Vite + React 18 + TypeScript + Tailwind v4 + react-router（**HashRouter**）+ vite-plugin-pwa。
存储：**OPFS**（图片文件）+ **IndexedDB**（元数据，用 `idb` 封装）。无第三方 EXIF/CV/压缩库以外的依赖。

```
src/
  types.ts              数据模型 + displayFile()（取最佳可用图）
  storage/db.ts         IndexedDB：courses / photos 两表，schema v2
  storage/opfs.ts       OPFS 文件 save/read/delete
  lib/exif.ts           手写 JPEG/PNG EXIF 时间解析（含单测）
  lib/matching.ts       拍摄时间 × 课表时间槽匹配（含单测）
  lib/crop.ts           边缘检测 + 透视校正 + 缩略图 + solveHomography/scaleQuad（含单测；2026-09-18 的实验改动已回滚，见 §7）
  lib/sessions.ts       按"哪一节课"分组（课程详情页时间线用）
  lib/backup.ts         zip 导出/导入合并
  components/CropEditor 四角拖拽裁剪编辑器（含 persistPhoto / updatePhotoCrop）
  components/Lightbox   大图查看（键盘翻页、原图/裁剪切换）
components/RecropDialog 对已入库照片重新裁剪
dev-tools/crop-regression.html 裁剪算法回归 harness（仅 dev 可访问，不进构建；fixture 在 dev-tools/fixtures/）
components/SideNav    宽屏左侧导航（lg 断点）
  pages/                课表 / 拍照 / 归档 / 课程详情 / 同步 / 课程表单
```

## 4. 数据存在哪里（接手前必须理解的一点）

照片和课表存在**用户浏览器自己的私有沙箱**（OPFS + IndexedDB），按网址隔离：

- 手机 Chrome 的沙箱 ≠ 电脑浏览器沙箱 ≠ 开发时 `localhost` 沙箱，三份互不相通
- 文件管理器看不到（安卓 11+ 隔离），云端**没有任何副本**
- 清浏览器缓存 / 换机 / 卸载 = 数据全丢

PC-first 之后只有**电脑那份沙箱是真源**（手机那份是冻结的备胎），因此「同步」页的完整备份包 = 唯一异地副本，**每月导出一次**的纪律比以前更重要，这个功能不是锦上添花，是数据安全的地基。

`PhotoMeta` 关键字段：`fileName`（原图）、`thumbFileName`（~512px 缩略图）、`cropFileName`（透视校正图）、`quad`（四角，原图像素坐标）、`takenAt`（EXIF 时间）、`courseId`（`''` = 未归属）、`capture`（`auto`/`manual`/`none`）、`backedUpAt`（被完整包收录时间）、`originalRemoved`（原图已清理）。

## 5. 已完成（都有验收标准，详见 PLAN.md 的勾选）

| 里程碑 | 内容 | 状态 |
|---|---|---|
| M1 | 脚手架、存储层、EXIF、页面骨架 | ✅ |
| M2 | 课程增删改查、时间槽、课表周视图 | ✅ |
| M3 | 拍照/相册导入、自动边缘检测+四角微调、透视校正、缩略图管线、schema v2 | ✅ |
| M4 | 时间戳自动归课（±10 分钟容差、重叠取覆盖最深）、待确认列表改归属 | ✅ |
| M5 | 桌面响应式（宽屏侧边导航+多列照片墙）、大图查看、课堂时间线、重新裁剪 | ✅ |
| 阶段1 | zip 导出（完整/精简）/导入去重、清理已备份原图、触屏删除修复+多选批量删 | ✅ |
| 阶段2 | 回滚裁剪实验改动、裁剪界面"整体收缩/放大"兜底按钮、jsWarp 单应修复+单测（共 18 个）、宽屏 1440px 实测通过、真实照片回归 harness+基线 | ✅ |
| 部署 | GitHub Pages，SW 已注册激活，可离线/加主屏幕 | ✅ |

真机反馈（用户实际在教室用过一次）：**自动归课成立**（核心假设通过）、**轻微卡顿**、**自动裁剪"框到了但框得太大"**（当前主要缺陷，见 §7）。

## 6. 关键决策与否决项（别重走弯路）

- **不做 OCR 转文字笔记**：公式/图表识别不可靠，转出来没法看。OCR 只以"搜索索引"身份留在 M7（识别错字只影响召回，可接受）。
- **不自建后端（现阶段）**：同步靠 zip。已定下一步走自建大陆服务器，见 §7 的清单。
- **不做 HEIC 兼容**：用户是安卓，iPhone 明确不考虑。
- **不套现成开源/商店 App**：调研过 classnotes-album（同思路但无 LICENSE、无人维护）、Slidestitch（MIT，录音对齐）、Smart-Note-Scanner（MIT，透视矫正思路）、Sleepy（GPL-3.0，活跃课表 App）。商店 App 闭源改不了；开源课表是 Kotlin 原生 Android，加相机+归档等于换语言重学；结论是自研 + 借鉴思路。详见 PLAN.md「标杆参考」。
- **不做埋点/管理后台**：个人项目范围失控的典型教训，见 classnotes-album。
- **不做 TF.js 图像分类判归属**：时间戳比它可靠。
- **不做手机端迭代、不做服务器同步（2026-09-18 定案）**：用户实际工作流是"手机纯当相机（EXIF 自带时间戳）、回宿舍电脑归档复习"，照片真源在电脑后跨设备同步问题不存在，服务器+备案路线随之取消。若未来真出现多设备需求，git 历史里的旧版 PLAN.md 有完整采购与技术方案存档，从那里恢复，别凭记忆重推。

## 7. 待办（按优先级）

### P0：裁剪"框太大"——**已按下方建议处理（2026-09-18），算法本身保持稳定版**

**2026-09-18 处理记录**：实验改动已 `git stash` 回滚（`git stash list` 可见，`git stash pop` 可找回，**不要直接提交它**）；裁剪界面已加"整体收缩/整体放大"兜底按钮（四角绕质心联动、按住可连续移动），用户遇到框偏大/偏小一按就能修好；`jsWarp` 回退的数学错误已修复并补单测。**仍欠：无（回归样本已就位）**。真实教室照片回归样本已入库：`npm run dev` 后打开 `http://localhost:<端口>/dev-tools/crop-regression.html`，页面自动跑 `detectQuad` 并与 `dev-tools/fixtures/crop-regression.json` 里的基线比对，**与基线 IoU ≥ 0.96 才算通过**；算法真变好了就点「生成基线 JSON」更新基线并在 JSON 里注明原因。合成图会过拟合（下方表格是教训），不要往回走。

当时实验改动的内容（留作根因分析参考）：候选轮廓改为按"内部亮度 × 形状方正度"打分、凸包+逐级多边形逼近替代 `minAreaRect`、新增 `snapQuadToEdges()` 边缘吸附。

**它现在比已提交版本更差，不要直接提交。** 用合成图（无光晕/软光晕/硬光晕三种，光晕=投影照亮幕布周围的墙）量化面积比（1.0 为准确）：

| 场景 | 已提交版 | 实验中（中间态） | 实验中（最新） |
|---|---|---|---|
| 硬光晕（用户报的毛病） | **1.85（太大）** | 0.96 ✅ | 1.87 ❌ |
| 无光晕 | 1.00 | 0.96 ✅ | 0.96 / 0.84 |
| 软光晕 | 1.09 | 0.72 ❌ 裁掉内容 | 0.72 ❌ |

已定位的根因：
1. 旧算法要求轮廓逼近**恰好 4 个角点**，真实屏幕轮廓常是 6~10 点 → 被丢弃，只剩外层光晕轮廓
2. 旧算法**按面积取最大**，于是选中被照亮的墙
3. 边缘吸附会被**幻灯片内部的文字/图形边缘**骗到（文字也是"向内变亮"），导致裁掉内容——**欠裁剪比过裁剪更严重**，不可接受

**建议做法（我推荐，也最省事）**：`git checkout src/lib/crop.ts` 回到稳定版，先只做**不依赖算法的兜底**——裁剪界面加"整体收缩/整体放大"按钮（四角同步向心/离心移动），用户一按就能修好框；然后**拿一张真实教室照片当回归样本**存进项目，之后任何算法改动都必须先通过它。**不要继续在合成图上反复调参**——三轮下来已经在过拟合，每次改一个 fixture 就弄坏另一个。

### ~~P0：服务器同步~~ 已取消（2026-09-18 PC-first 转向）

照片真源改为电脑后不存在第二座数据孤岛，同步问题消失。采购清单、备案材料与 6 条技术约束的全文在 git 历史的旧版 PLAN.md，要点与重启条件见 PLAN.md「跨设备数据方案」。备份降级为**每月导出完整 zip 扔网盘/移动硬盘**的纪律。

### P1：PC-first 待验证清单（需要用户配合）

1. **EXIF 经传输保留验证（新链路的第一个假设）**：手机拍一张 → 按日常方式传到电脑 → 电脑端导入 → 确认自动归课命中。微信以"图片"方式发送会压缩剥 EXIF；用数据线、LocalSend 或微信"文件"方式
2. **每月备份纪律**：同步页导出完整 zip → 网盘/移动硬盘（唯一副本在浏览器沙箱）
3. 固定用一个电脑浏览器打开使用（Chrome/Edge 二选一，两边的沙箱互不相通）

### P2：小遗留

- 备份包无完整性校验（坏包会被拒，但不告诉你哪张坏了）
- 课程详情页无批量操作（多选删除只在归档页）
- 图片文件丢失时大图页会提示"文件已丢失"（已修），但列表页仍显示占位
- M7 OCR 搜索索引（Tesseract.js，等用户真出现"记得内容翻不到"再做）
- ~~备案通过后页脚挂备案号~~（2026-09-18 服务器路线已取消）

## 8. 踩过的坑（务必知道，都会再踩一次）

1. **`@techstark/opencv-js` 是 UMD**：Vite 下 `await import()` 拿到的 `default` 是 Emscripten 就绪 Promise，**必须再 await 一次**才是带 `Mat` 的实例；`window.cv` 那条路拿不到。它产物是 15MB 单 JS（WASM 内嵌 base64），已做懒加载 + SW 运行时缓存（`globIgnores` 排除出预缓存，否则构建失败）。
2. **EXIF 解析**：JPEG APP1 布局是 `FF E1 | len(2B) | "Exif"(4B) | 00 00 | TIFF`，TIFF 头在段起始 **+12**；`Exif\0\0` 是 6 字节。相机时间是**本地时区**，不能用 `Date.parse`（无时区串按 UTC）。M1 时没带 EXIF 的测试图，这个 bug 潜伏到 M4 才暴露——**教训：解析类代码必须先造二进制 fixture**。
3. **IndexedDB 索引不接受空字符串外的非法键**；`by-course` 索引用 `sparse: true`，TS 的 `IDBIndexParameters` 缺该字段需断言。
4. **PWA 更新要重启两次**：SW 后台静默换新，第二次启动才是新版。用户报"没有同步页"多半是这个。
5. **GitHub Pages 不支持 SPA rewrite** → 用 HashRouter + `base: './'` + manifest `start_url: './'`。
6. **gh token 无 `workflow` scope** → 不能推 `.github/workflows/`，故改用 gh-pages 分支发布（备用 workflow 文件在 `.deploy-backup/`，已 gitignore）。
7. **Qoder CN 内置浏览器遇到原生 `confirm()` 或下载会整个标签页卡死**，`handle_dialog` 也超时；只能人工作别关掉，或远程点掉。**验证涉及确认弹窗的功能时，改用直接调用被测函数**（本项目阶段1/触屏删除就是这么"漏测"的——逻辑本身没问题，但 UI 层弹窗没自动化验证过）。
8. **触屏没有 hover**：M1 把删除按钮写成 `group-hover:flex`，手机上永远看不到。凡是只靠 hover 的交互在移动端都是坑。

## 9. 协作约定（用户偏好，别忽略）

- **先聊再动手**：非琐碎任务先给计划和取舍，等确认；花钱或架构岔路要先讲透
- 计划写进 `PLAN.md`，**完成一项就划勾**，里程碑带日期
- **提交和部署要单独授权**，不要顺手 push
- 在意目录整洁：装依赖/建临时文件后要说明落在哪、多大、怎么清；用完即删（临时测试图、脚本都已清理，不要留在仓库里）
- 用户是安卓手机、在校大学生、对前端工具链内部机制不熟：**要主动交代"数据在哪、怎么删"**，别假设他懂 OPFS/IndexedDB
