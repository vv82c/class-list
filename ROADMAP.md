# 未来计划（Roadmap）

> 写于 2026-09-19。当前状态：**本学期的功能闭环已经完成**（阶段 1~7，见 PLAN.md），项目进入"真实使用驱动开发"模式——下面每一项都**不排期**，由真实使用中的痛点触发启动。想做什么之前，先问：这个问题我真的遇到了吗？

## 现状一览（2026-09-19）

已闭环的能力：课表录入（含单双周/周次范围）→ 手机拍照 → 电脑导入（拖拽/文件夹）→ 自动边缘检测 + 透视校正 + 画质增强 → 按课表自动归课到「第几堂」→ 大屏复习（缩放/连播/周次与标星过滤）→ 荧光笔等标注 → 导出带标注图片 → zip 备份。

规模：约 5700 行源码，43 个 lib 层单测，已部署 GitHub Pages。当前已知问题与验收缺口见 HANDOFF.md §7。

## 下一批：OCR 搜索（M7，独立里程碑）

**触发条件**：照片量堆起来之后，真的出现"记得讲过这个内容，但翻不到"的情况。

**设计要点**（已议定，启动时照此执行）：
- Tesseract.js 本地识别（中英文），不上传任何服务器
- 后台闲时队列：入库后排队识别，可中断可续跑（每张记已索引标记），不阻塞正常使用
- 语言包走 SW 运行时缓存（首次需联网下载一次，之后离线可用）
- 搜索范围：识别文本 + 照片备注 + 文字框标注，按课程过滤；v1 用包含匹配（错字只影响召回，可接受）
- 输入质量现成：裁剪图已透视校正 + 增强，OCR 准确率白拿一截

预估 250~350 行，独立成一个阶段做。

## 按需小项（真实需求出现再做）

| 项 | 说明 | 为什么现在不做 |
|---|---|---|
| 笔迹部分擦除 | 橡皮擦按几何切割只擦半条线 | 整条删 + Ctrl+Z 已覆盖绝大多数场景，几何切割成本高 |
| 旧照片批量重增强 | 一键给增强管线上线前入库的照片补增强 | 存量就几张，大图页「重新裁剪」逐张补即可 |
| 课堂导出 PDF | 一堂课的照片合成打印版 | 不打印就不需要 |
| 备份包完整性校验 | 坏包提示具体哪张损坏 | 低频，现有"坏包拒绝"够用 |
| 课程详情页批量操作 | 多选删除/确认 | 归档页已有，详情页用到再说 |

## 远期备选（不排期，条件触发）

- **课堂录音对齐**：拍照时间戳对齐录音进度，点照片跳转当时讲的录音（复用教学周/时间匹配逻辑，差异化最强）
- **教务系统课表导入**：免手录课表
- **跨学期学期列表**：settings 支持多条学期起点记录（现在单学期，寒假切换时改起点即可；在意跨学期编号准确性时升级）
- **多端实时同步**：重启条件 = 真出现"多台设备都要写入"的需求；完整服务器方案（含备案、采购、协议设计）在 git 历史的旧版 PLAN.md，从那里恢复，不要凭记忆重推

## 运维纪律（比写新功能更重要）

- **每月**：备份页导出完整备份包 → 网盘/移动硬盘（数据唯一副本在浏览器沙箱里）
- **每学期初**：更新「本学期第一周」→ 核对各课时间槽周次（单双周）→ 归档页点一次「重新归课」
- **始终**：固定用一个电脑浏览器（Chrome/Edge 二选一，沙箱互不相通）；任何裁剪算法改动先过 `dev-tools/crop-regression.html` 回归门槛
- 传输照片用数据线 / LocalSend / 微信「文件」方式，**不要**用微信「图片」方式（压缩会剥 EXIF，自动归课就瞎了）

## 已明确砍掉（别再提）

- 服务器同步 / 域名备案（2026-09-18 取消，重启条件见 PLAN.md「跨设备数据方案」）
- 手机端 UI（2026-09-18 砍除，手机只当相机）
- OCR 转文字笔记（只做搜索索引）、账号系统、iOS/HEIC、埋点与管理后台
