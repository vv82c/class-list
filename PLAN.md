# 课堂照片归档 App — 施工计划

> 每完成一项任务，把对应的 `[ ]` 改成 `[x]`；里程碑全部完成后再勾选里程碑标题。

## 产品定位

移动优先的 PWA（无后端，本地存储）：
手机拍照/导入 → 自动检测 PPT/黑板边缘并裁剪（可手动调四角） → 按拍摄时间自动对齐课表、归到对应课程 → 电脑上同一网址查看归档结果。手机↔电脑通过"导出数据包 / 导入合并"同步。

MVP 明确**不做**：OCR 转文字笔记、账号系统、教务系统自动导入、HEIC 兼容（用户为安卓，iPhone 暂不考虑）。

## 技术选型（2026-09-18 对照开源标杆项目修订）

- Vite + React + TypeScript，PWA（vite-plugin-pwa，可加主屏幕、离线可用）
- 图片文件存 OPFS，元数据存 IndexedDB（idb 封装）
- **缩略图管线**：导入时用 Canvas 把原图缩放至最长边 ~512px 另存一份，列表/照片墙只读缩略图（防止全尺寸图解码拖垮手机滚动）
- **裁剪检测**：优先 `@techstark/opencv-js`（WASM 版，含 SIMD 优化）做文档边缘检测，失败时回退手动四点选区；透视校正后按四边形实测边长还原宽高比（借鉴 Smart-Note-Scanner，MIT），中间留 `DocumentDetector` 抽象接口
- **OCR（仅 M7 搜索索引）**：Tesseract.js 中英文识别，结果只入搜索索引不给人读——识别错误只影响召回，可接受（classnotes-album 验证过此定位）
- UI：Tailwind CSS

## 数据模型

- `Course`：id、课程名、颜色、时间槽列表（星期几 + 起止节次/时间）
- `PhotoNote`：id、原图句柄、缩略图句柄（~512px）、裁剪图句柄、四角坐标、拍摄时间（EXIF）、归属课程 id、归属方式（auto/manual/none）、备注、OCR 索引文本（M7 起）
- `ScheduleSlot`：星期 + 开始/结束时间，归属于某 Course

---

## M1：地基 ✅ 2026-09-18

- [x] Vite + React + TS 项目初始化，配置 Tailwind、路由、PWA manifest
- [x] IndexedDB 层（idb）：courses / photos 两张表的 CRUD 封装
- [x] OPFS 图片存储封装：save / read / delete，文件名与 PhotoNote 关联
- [x] EXIF 拍摄时间提取工具函数
- [x] 应用骨架页面：课表页 / 拍照页 / 归档页 / 课程详情页（空壳 + 导航）
- [x] **验收**：`npm run dev` 起得来，手机访问局域网地址能看到四个页面并能切换，控制台无报错

## M2：课表录入 ✅ 2026-09-18

- [x] 课程增删改查界面（名称、颜色）
- [x] 每周时间槽编辑（星期几 + 起止时间）
- [x] 课表周视图（按星期×节次渲染）
- [x] **验收**：录入 5 门课的真实课表，周视图显示正确；杀掉 app 重开数据还在

## M3：拍照 → 裁剪 ✅ 2026-09-18

- [x] 相机/相册导入（input capture + multiple），批量处理队列
- [x] 导入时生成 ~512px 缩略图；IndexedDB schema 升 v2（新增缩略图/裁剪图字段，兼容旧数据）
- [x] WASM OpenCV 自动边缘检测 + 裁剪界面的四角拖拽微调（检测失败回退纯手动选区）
- [x] 透视校正裁剪（含宽高比还原），生成裁剪图并保存 OPFS（原图也保留）
- [x] **验收**：在教室斜拍一张 PPT，自动框出边缘，拖动四角调整后保存；重启 app 后裁剪图和原图都在；导入 50 张照片后归档页滚动流畅不白屏

> 实施备注：@techstark/opencv-js 实际产物为 15MB 单 JS（WASM 内嵌 base64），已做懒加载 + SW 运行时缓存（不进预缓存）；首次检测需下载一次，之后离线可用。真机弱网下的首次体验待实测。

## M4：自动归档（核心） ✅ 2026-09-18

- [x] 拍摄时间 × 课表时间槽匹配算法（含 ±10 分钟容差、多课重叠时段判定）
- [x] 匹配成功自动归课；失败的进"未分类"待处理
- [x] 归档确认列表：批量确认自动归属的结果，手动改错归
- [x] **验收**：连着两堂课各拍几张，导入后大部分照片自动归到正确课程，错归/未归的能在确认列表里手动纠正

> 实施备注：M1 的 EXIF 解析器存在段偏移 bug（此前无带 EXIF 的测试图未暴露），M4 补了合成 EXIF 单测并修复；匹配算法与 EXIF 解析共 11 个单测，`npm test` 可跑。

## M5：复习视图（2026-09-18 按"宿舍电脑复习"重定范围）

用户真实使用循环是**教室用手机拍、回宿舍用电脑大屏看**，因此 M5 从"手机大图页"改为"桌面复习界面"：

- [x] 桌面响应式布局：宽屏去掉 512px 手机窄条，改多栏（左侧课程导航 + 右侧照片墙），列数随屏宽自适应 ✅ 2026-09-18：`lg` 断点切侧边导航，照片墙 3→4→5→6 列
- [x] 大图查看（复习主战场）：键盘 ← → 翻页、Esc 关闭、显示归属课程与拍摄时间、原图/裁剪图切换 ✅ 2026-09-18
- [x] 课程详情页改为按"哪一节课"分组的时间线（日期 + 节次 + 张数），可折叠 ✅ 2026-09-18：`lib/sessions.ts` 按课程时间槽分组
- [x] 大图页提供"重新裁剪"入口（自动检测不可靠时的兜底手段，复用裁剪编辑器写回原记录）✅ 2026-09-18：写回裁剪图+重建缩略图，并清除 backedUpAt 防止误清理
- [x] 未分类照片的兜底浏览入口 ✅（归档页"未分类"筛选已具备）
- [x] **验收**：电脑浏览器打开后能一屏看到多张大缩略图、键盘翻页复习；点开任意一门课按课堂分组显示；对某张重新裁剪后结果覆盖旧裁剪图

> 实测记录：窄屏 724px 下照片墙为 4 列、时间线分组与翻页正常、重裁写回生效；顺带修了一个真 bug——图片文件缺失时大图页会永远停在"读取中…"，现改为退回可用文件并提示"文件已丢失"。
> **未验证**：≥1024px 宽屏的侧边导航（测试浏览器视口最宽 724px），需你在电脑上打开一次确认。

## M6：同步 + 打磨（部署部分已提前完成）

- [x] 一键导出数据包（zip：图片 + 元数据 json）✅ 2026-09-18：两档——完整包（含原图，兼作唯一异地备份）/ 精简包（仅裁剪图+缩略图，体积小很多）
- [x] 另一端导入：合并去重 ✅ 2026-09-18：按 id 去重、本地为准；已验证导出→清空→导入字节一致、重复导入全部跳过
- [x] 附加：清理已备份原图（保留裁剪图/缩略图）+ 同步页存储占用统计
- [x] 修复：归档页删除按钮原为 hover 显示，触屏不可见 → 改为常驻，并新增多选批量删除
- [x] PWA 离线缓存验证、真机添加到主屏幕（SW 已注册激活；主屏幕添加待真机确认）
- [x] 部署 ✅ 2026-09-18：**https://vv82c.github.io/class-list/**（GitHub Pages，gh-pages 分支 + gh-pages CLI 发布，无需 Actions；HashRouter + base './' 适配子路径；推 main 后需手动跑 `npm run build && npx gh-pages -d dist` 发布）
- [ ] **验收**：手机上整理好的数据，导出 → 电脑导入后完整可见（待真机跑通一次）

---

## 跨设备数据方案（2026-09-18 定案，供后续阅读者理解现状）

### 数据现在在哪里

照片文件存 **OPFS**、课表与归属存 **IndexedDB**，两者都按"网址"隔离，落在**当时操作的那台设备的浏览器私有沙箱**里：

- 手机 Chrome 的沙箱 ≠ 电脑浏览器的沙箱 ≠ 开发时 `localhost` 的沙箱，三份互不相通
- 云端没有任何副本；文件管理器进不去（安卓 11+ 隔离）
- 清浏览器缓存 / 换机 / 卸载 = 数据全丢，因此**完整备份包是唯一的异地副本**

### 现阶段（已上线）：zip 手动搬运

手机「同步」页导出 → 聊天软件或数据线传到电脑 → 电脑「同步」页导入。约 20~30 秒一次，**手机为唯一真源，电脑只读复习**。

### 下一步：自建大陆服务器同步（已选定，待执行）

选它的理由：三年自用体量（一学期约 1GB，三年 6~8GB）会撑爆免费网盘/GitHub 方案；国内网络稳定；数据自主；40GB 磁盘 38~68 元/年最划算。

**采购清单**
- 服务器：阿里云/腾讯云 轻量应用服务器 2C2G / 40GB / 200Mbps 不限流量，大陆地域（杭州/上海/北京/深圳），38 元/年（阿里每日 10:00、15:00 秒杀；抢不到 68 元直接买；腾讯新人 38 元档常驻）
- 域名：建议 `.cn`（首年 20~30、续费约 30）；避开 `.xyz`（首年几元但续费 30~90）
- 备案：服务器须包年包月且剩余 ≥3 个月；域名先实名认证（1~2 天，主体须与备案人一致）；材料为身份证 + 人脸核验 + 手机号，**人在学校就读但户籍在外省时需学生证/学籍验证报告**；周期约 1 个月（初审 1~2 天 + 管局 7~20 工作日）；通过后页脚挂备案号

**关键技术约束（务必遵守）**
1. 前端已在 GitHub Pages（HTTPS），HTTPS 页面**不能**请求 HTTP 接口（混合内容被浏览器拦截）→ 后端必须 HTTPS → 必须有域名 + 证书（Caddy 自动签发续期）
2. 大陆服务器 + 域名解析 ⇒ 必须备案，没有绕开的余地
3. API 要**客户端无关**：Bearer token 鉴权、不依赖 cookie，将来 TWA 打包或原生壳可直接复用
4. 同步协议需要 schema v3：`updatedAt`（后写赢）、删除墓碑（否则无法区分"对方删了"与"还没同步"）、`syncState` 变更队列
5. **备份不能省**：cron 把 SQLite + 照片打包推异地处；另加宕机监控。自用工具最常见的死法是服务器静默挂掉
6. 导出 zip 能力永久保留，作为退路与最终可迁移性保证

**时间线**：备案等待的约 1 个月内并行做 M5 与同步层（不依赖服务器），域名下来后部署服务端 API + 配对联调。

---

## M7：OCR 搜索索引（M6 之后按需启动）

- [ ] Tesseract.js 接入：照片入库后空闲时后台识别（中英文），结果存入 PhotoNote 索引字段
- [ ] 全局搜索框：关键词命中板书/PPT 文字，按课程过滤结果
- [ ] **验收**：输入"第三章"之类的关键词，能找到含该字样的课堂照片（允许 OCR 有错字，命中即可）

---

## 后续备选（不排期）

- **课堂录音对齐**（Slidestitch 思路）：录音起始时间 + 照片 EXIF 时间 = 每张图在音频中的偏移，点照片跳转当时录音；复用 M4 时间匹配逻辑，成本低、差异化强
- 教务系统课表自动导入
- 照片内作业内容的标注/提醒
- 多端实时同步：到那时再评估 Supabase（classnotes-album 验证过该组合可行），现阶段用导出/导入顶住

## 标杆参考（2026-09-18 调研）

- [classnotes-album 课堂相册](https://github.com/roseefeng/classnotes-album)：时间戳+OCR 自动归课、Tesseract.js 搜索索引、Supabase 同步形态（注意：无 LICENSE，只参考思路不复用代码）
- [Slidestitch](https://github.com/kk8bit/Slidestitch)（MIT）：EXIF 时间戳对齐录音与 PPT 照片
- [Smart-Note-Scanner](https://github.com/agptz483/Smart-Note-Scanner)（MIT）：OpenCV 四点选区 + 透视矫正 + 宽高比还原
- [Sleepy](https://github.com/lingion/sleepy)（GPL-3.0）：活跃课表 App，周视图交互可参考
- 戒律：个人项目勿加埋点/管理后台（classnotes-album 的范围失控教训），TF.js 图像分类不如时间戳可靠，不跟进
